{
  "name": "system-sync",
  "version": "0.1.0",
  "private": true,
  "description": "System Sync - two player card game (server + client)",
  "main": "dist/server/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "eslint . --ext .ts,.tsx,.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server/index.ts",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.5",
    "@types/node": "^20.4.2",
    "@types/cors": "^2.8.14",
    "jest": "^29.6.0",
    "ts-jest": "^29.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
